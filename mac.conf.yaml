- shell:
    -
      command: sh ./scripts/mac-pre-install.sh
      description: Running --mac-- pre installation script
      stderr: true
      stdout: true
      stdin: true
    - 
      command: brew bundle
      description: Installing Homebrew formulas from Brewfile
      stderr: true
      stdout: true

- link:
    ~/Brewfile: Brewfile
